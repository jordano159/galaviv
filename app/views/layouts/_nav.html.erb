<nav data-controller="nav">
    <div class="menu">
    <h1><%= link_to 'יויו ונעה מתחתנים',root_path %></h1>
        <button class="hamburger hamburger--squeeze" type="button" data-action="nav#menu" data-nav-target="btn">
            <span class="hamburger-box">
                <span class="hamburger-inner"></span>
            </span>
        </button>
    </div>

        <ul role="list" data-nav-target="list">
            <li><%= link_to 'דף הבית',root_path, class: "#{"active" if action_name == "home"}" %> </li>
            <li><%= link_to 'מהי חתונה חברתית', social_wedding_path, class: "#{"active" if action_name == "social_wedding"}" %></li>
            <li><%= link_to 'אישור הגעה', arrival_path, class: "#{"active" if action_name == "arrival"}" %></li>
            <li><%= link_to 'מיקום', location_path, class: "#{"active" if action_name == "location"}" %></li>
            <li><%= link_to 'תפריט', menu_path, class: "#{"active" if action_name == "menu"}" %></li>
            <li><%= link_to 'התנדבות', volunteer_path, class: "#{"active" if action_name == "volunteer"}" %></li>

            <% if admin_signed_in? %>
              <li><%= link_to 'דף ניהול', admin_path %></li>
              <!--
              <li><%= link_to 'התנתקות', destroy_admin_session_path, method: :delete %></li>
              -->
            <% end %>
        </ul>
</nav>
<% if heb_action_name.present? %>
    <h1 class="page-title">
        <%= heb_action_name %>
    </h1>
<% end %>